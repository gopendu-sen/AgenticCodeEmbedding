# Ops service config. Embedding base URL points to upstream embedding provider.
extends: "./config.common.yml"

embedding:
  # Upstream embedding provider used by ops ingestion/evaluation.
  base_url: "http://localhost:11434/v1"

chat:
  api:
    port: 8026
